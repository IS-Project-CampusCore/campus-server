syntax = "proto3";

import "Protos/common.proto";

option csharp_namespace = "someServiceClient";

package someService;

service someService {
	rpc DoSomething (DoSomethingReq) returns (commons.protos.ProtoMessageResponse);
	rpc DoSomethingElse (DoSomethingReq) returns (commons.protos.ProtoMessageResponse);
}

message DoSomethingReq{
	string someReqMessage = 1;
}

message DoSomethingRes{
	string someResMessage = 1;
}